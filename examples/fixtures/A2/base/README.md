# Signup service

Run the tests with `python -m unittest`. One test in `tests/test_report.py` is a known failure, tracked separately.

Open defect: the signup form accepts an email without a domain, such as `anna@`.
