# Learnings

<!-- One row per insight that saves a fresh agent a wrong turn: a pitfall, a
     project quirk, a command fix, a pattern that held. Never restate an
     artifact. Prune a row when the files it names no longer exist. Keep at
     most 30 active rows: this file is read at every route start; move the
     rest to Archive, which is never loaded. -->

| Date | Type | Insight | Files | Source |
|---|---|---|---|---|
|  | `pitfall \| quirk \| command \| pattern` |  |  | `route or module` |

## Archive

<!-- Rows no longer worth a route-start read. -->
