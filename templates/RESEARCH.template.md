# Research Ledger

<!-- One row per gap resolved by `modules/research.md`, newest first. Read for
     deduplication before any fan-out; the earliest due revisit trigger is
     mirrored in STATE.md. Raw research never enters this file: answer, label,
     source only. Keep at most 40 active rows; move rows whose trigger fired or
     whose consumer no longer exists to Archive, which is never loaded. -->

| GAP | Question | Answer | Label | Source (date) | Revisit when | Consumed by |
|---|---|---|---|---|---|---|
| GAP-001 |  |  | `FACT \| INFERENCE \| ASSUMPTION` |  |  |  |

## Archive

<!-- Superseded rows, kept for provenance only. -->
